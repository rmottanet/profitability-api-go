package services_test

//import (
    //"testing"
    //"time"

    //"profitability/api/pkg/services"
    //"profitability/tests/pkg/mocks"
//)

//func TestPropService_Prop(t *testing.T) {
    //// Criando um mock para o cache
    //mockCache := mocks.NewMockCache()

    //// Definindo um valor para a taxa SELIC no cache
    //mockCache.Set("SELIC", 0.1, 1*time.Hour)

    //// Criando uma instância do serviço de taxa proporcional
    //propService := services.NewPropService(mockCache)

    //// Testando o cálculo da taxa proporcional
    //result, err := propService.Prop(0.2)
    //if err != nil {
        //t.Errorf("Erro inesperado: %v", err)
    //}

    //expectedResult := 0.02 // 0.2 * 0.1
    //if result != expectedResult {
        //t.Errorf("Resultado incorreto. Esperado: %f, Obtido: %f", expectedResult, result)
    //}
//}
